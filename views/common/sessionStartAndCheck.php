<?php

//--- generate or get the session id.
session_start();

# if ( isset($_SESSION['user_rowid']) && isset($_SESSION['user_login']) && isset($_SESSION['user_fk_group_rowid']) ) {
	/*	
	$user = new User();
	$user_rowid = (int) $_SESSION['user_rowid'];
	$user->load($mysqli, $user_rowid);
	*/

# }

?>